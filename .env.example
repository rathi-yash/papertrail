# Optional. If set, PaperTrail uses Claude for extraction (best quality).
# If unset, it falls back to a local open-weight model (free, lower accuracy
# on messy or ambiguous paper text).
ANTHROPIC_API_KEY=
